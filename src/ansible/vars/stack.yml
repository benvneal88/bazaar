stacks:
  - stack_name: bz-traefik
    env: dev
    services:
      - name: reverse-proxy
        image: traefik:v3.0
  - stack_name: bz-status
    env: dev
    services:
      - name: status
        image: traefik/whoami
        replicas: 1
  # - stack_name: bz-it-tools
  #   env: dev
  #   services:
  #     - name: it-tools
  #       image: corentinth/it-tools
        
  #       replicas: 1
